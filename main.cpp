#include <iostream>
#include <set>
#include <vector>
#include <algorithm>
using namespace std;

void doicho(int a[2])
{
    int temp = a[0];
    a[0] = a[1];
    a[1] = temp;
}

int main() {
    int a[2] = {5, 10}; // mảng với 2 giá trị là 5 và 10
   for (auto& it = a.begin(); it != a.end() ;it++ ){
    
   }
}


// callback